apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: model
  name: model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model
  strategy: {}
  template:
    metadata:
      labels:
        app: model
    spec:
      containers:
      - image: pushpendras21/house-pricemodel:latest
        name: house-pricemodel
        ports:
        - containerPort: 8000
        resources: {}
status: {}
